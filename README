Free Code Camp Pomodoro Clock project.  This version does not use Bootstrap or React.

It may have FCC's test bundle (currently in Beta) linked in, but that can be commented out
in index.html and things will still work.

